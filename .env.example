# ===========================================
# SaaS Dashboard - Environment Configuration
# ===========================================
# Copy this file to .env.local for local development
# For production, set these in your hosting provider (Vercel, etc.)

# -------------------------------------------
# DATABASE CONFIGURATION
# -------------------------------------------

# OPTION 1: Local Development with Docker
# Use this when running PostgreSQL locally via docker-compose
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/saas_dashboard"

# OPTION 2: Production with Supabase
# IMPORTANT: For Vercel/serverless, you MUST use the Transaction Pooler URL
#
# Transaction Pooler URL (port 6543) - USE THIS FOR VERCEL
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
#
# Direct URL (port 5432) - Only for local development & migrations
# DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
#
# How to find your Supabase connection strings:
# 1. Go to Supabase Dashboard → Project Settings → Database
# 2. Look for "Connection string" section
# 3. For pooler URL: Select "Transaction" mode from the dropdown
# 4. For direct URL: Select "Direct" or use the db.[ref].supabase.co format

# -------------------------------------------
# AUTHENTICATION (NextAuth.js)
# -------------------------------------------

# Secret key for JWT signing - REQUIRED
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-a-real-one"

# Base URL of your application
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# -------------------------------------------
# OPTIONAL CONFIGURATION
# -------------------------------------------

# Direct database URL for Prisma migrations (when using pooler for main connection)
# DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Public app URL (optional, defaults to NEXTAUTH_URL)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
